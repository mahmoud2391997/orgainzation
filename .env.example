# PostgreSQL connection string. Examples: local Postgres or a managed provider.
DATABASE_URL=postgresql://postgres:password@localhost:5432/antitude

# Set a strong secret in every non-local environment.
ADMIN_PASSWORD=replace-with-a-strong-password

# Optional connection pool size.
DATABASE_POOL_MAX=5
